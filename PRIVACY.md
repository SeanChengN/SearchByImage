# 隐私与数据说明

## 图片

- 应用只处理用户通过系统分享或照片选择器明确提供的图片。
- 原图先复制到应用私有缓存；上传副本会限制最长边、修正方向并重新编码，从而移除 EXIF、定位和设备信息。
- 每次只向用户明确选择的一个搜索服务发送一张图片。
- 缓存文件保留不超过 24 小时，并可由 Android 在存储紧张时清理。

## 搜索服务

- 自动 API：trace.moe、SauceNAO、Lenso.ai、TinEye。
- 外部应用：Google Lens/Google 应用。
- 网页辅助：Bing、Yandex、百度、搜狗、360、IQDB、Ascii2D。
- 网页辅助服务会在浏览器 Custom Tab 中打开，并要求用户再次选择图片；应用不注入脚本，也不读取浏览器 Cookie。

图片提交后由所选第三方服务的隐私政策和条款管理。应用会在引擎目录中标注自动、网页辅助、需密钥、收费或登录等状态。

## API 凭据

- 凭据由 Android Keystore 生成的设备密钥使用 AES-GCM 加密。
- 凭据只保存在本设备，不参与系统备份，不写入日志，也不会回显现有明文。
- 应用没有自有后端，不会代用户保存或转发凭据。

## 网络和遥测

- 所有应用发起的请求只允许 HTTPS，清单禁止明文流量。
- 自定义端点拒绝回环、私网、链路本地地址、非 443 端口和未验证重定向。
- 应用不包含 Firebase、崩溃上报、使用分析、广告或账户系统。
